# =============================================================================
# Beam Deployment Configuration
# =============================================================================
# This file contains all environment variables needed to configure the Beam
# media streaming platform for deployment via Docker/Podman Compose.
#
# Copy this file to `.env` and customize the values for your deployment.
# =============================================================================

# -----------------------------------------------------------------------------
# Postgres Database Configuration
# -----------------------------------------------------------------------------
# Name of the database user
POSTGRES_USER=beam
# Password for the database user
POSTGRES_PASSWORD=password
# Name of the database to create
POSTGRES_DB=beam
# Host port to expose Postgres on (mapped to 5432 in container)
POSTGRES_HOST_PORT=5432

# -----------------------------------------------------------------------------
# Auth & Session Configuration
# -----------------------------------------------------------------------------
JWT_SECRET=change_me_in_prod_please_use_a_strong_secret
REDIS_URL=redis://valkey:6379

# -----------------------------------------------------------------------------
# Beam Stream Service
# -----------------------------------------------------------------------------
# Port to expose the streaming service on the host
STREAM_HOST_PORT=8000
# URL where the stream service is accessible (used for links)
SERVER_URL=http://localhost:8000
# Database connection string for the service
DATABASE_URL=postgres://beam:password@postgres:5432/beam
# Logging level (info, debug, trace, etc.)
RUST_LOG=beam_stream=info,tower_http=debug,axum=debug
# Directory in the container where videos are stored
VIDEO_DIR=/videos
# Directory in the container where cache/temp files are stored
CACHE_DIR=/cache
# Host path to mount for video storage
HOST_VIDEO_DIR=./stream_videos
# Host path to mount for cache storage
HOST_CACHE_DIR=./stream_cache
# Enable Prometheus metrics endpoint
ENABLE_METRICS=false

# -----------------------------------------------------------------------------
# Beam Web Service (Frontend)
# -----------------------------------------------------------------------------
# Port to expose the web interface on the host
WEB_HOST_PORT=8080
# Application title
C_APP_TITLE=Beam
# URL of the stream server (backend) that the frontend connects to
C_STREAM_SERVER_URL=http://localhost:8000
