name: CI

# Every job body is a `mise run <task>`. The commands themselves live in
# mise.toml, which the git hooks (hk.pkl) invoke too, so CI and the hooks
# cannot drift apart. Add a check by adding a task, not by adding a `run:`.

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read

jobs:
  # Path filters gate the expensive jobs. Jobs skipped this way report as
  # "skipped", which the ci-ok aggregator treats as success -- that is what
  # makes a single required status check safe with conditional jobs.
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    outputs:
      rust: ${{ steps.filter.outputs.rust }}
      ts: ${{ steps.filter.outputs.ts }}
      docs: ${{ steps.filter.outputs.docs }}
      ffmpeg: ${{ steps.filter.outputs.ffmpeg }}
      ffmpeg_image: ${{ steps.filter.outputs.ffmpeg_image }}
      compose: ${{ steps.filter.outputs.compose }}
      android: ${{ steps.filter.outputs.android }}
      apple: ${{ steps.filter.outputs.apple }}
    steps:
      - uses: actions/checkout@v7
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            tooling: &tooling
              - 'mise.toml'
              - '.github/workflows/ci.yml'
              - '.github/actions/**'
            rust:
              - *tooling
              - '**/*.rs'
              - '**/Cargo.toml'
              - 'Cargo.lock'
              - 'rust-toolchain.toml'
              - 'deny.toml'
            ts:
              - *tooling
              - '**/*.ts'
              - '**/*.tsx'
              - '**/*.js'
              - '**/*.mjs'
              - '**/*.cjs'
              # Scoped to beam-web so that editing only beam-docs' .astro templates does not
              # drag the whole ts chain -- including the Rust/FFmpeg OpenAPI build that
              # ts-typecheck depends on -- into a docs-only change. beam-docs is covered by
              # the docs filter and the docs-build job. Its other files (.ts, .mjs,
              # package.json) still match the patterns above and still trigger ts, which is
              # correct: Biome lints beam-docs' .ts sources. beam-web has no .astro files
              # today; the pattern is here so that it would be covered if it gained any.
              - 'beam-web/**/*.astro'
              - '**/tsconfig*.json'
              - 'biome.json'
              - '**/package.json'
              - 'bun.lock'
              # The typed client is generated from the server's OpenAPI spec, so
              # changes to the API surface can break the TypeScript build.
              - 'beam-auth/src/**/*.rs'
              - 'beam-server/src/**/*.rs'
            docs:
              - *tooling
              - 'beam-docs/**'
              # beam-docs resolves its dependencies through the root lockfile, so a change
              # there can alter what the site builds against even when beam-docs/ is
              # untouched. Without this, the first sign of a break would be release.yml's
              # docs-deploy -- after the tag.
              - 'bun.lock'
            android:
              - *tooling
              - 'beam-android/**'
              # The Kotlin bindings and the .so are generated from the core, so
              # a change there can break the Android build without touching a
              # single Kotlin file.
              - 'beam-client-core/**'
              - 'Cargo.lock'
            apple:
              - *tooling
              - 'beam-apple/**'
              # The Swift bindings and the XCFramework are generated from the
              # core, so a change there can break the Apple build without
              # touching a single Swift file.
              - 'beam-client-core/**'
              - 'Cargo.lock'
              # Every apple-* job cross-compiles for the five Apple triples
              # declared here, so a change that drops one -- or moves the
              # channel -- must run them. Without this it matches only the
              # `rust` filter, the apple jobs report "skipped", ci-ok counts
              # skipped as success, and the break surfaces on some later,
              # unrelated pull request.
              - 'rust-toolchain.toml'
              # The app's version comes from here (ADR-0009).
              - 'version.txt'
            ffmpeg:
              - *tooling
              - 'Cargo.toml'
              # `ffmpeg-next = "9.0"` is a caret requirement, so the manifest
              # string alone does not pin the FFmpeg that gets vendored:
              # ffmpeg-sys-next's minor IS the FFmpeg minor its build.rs clones,
              # and that minor is decided here. A lockfile-only bump to 9.1 has
              # to run check:ffmpeg-version, which reads the resolved version.
              - 'Cargo.lock'
              - 'beam-server/Containerfile'
              - '.github/actions/setup-ffmpeg/action.yml'
            # Separate from `ffmpeg` above, and deliberately without *tooling:
            # this one gates ffmpeg-build, whose first act is resolving
            # `debian:trixie-slim` from Docker Hub, unauthenticated, from a
            # pooled runner IP. Under the shared filter every routine mise.toml
            # or ci.yml edit paid for that pull only to replay a cache hit and
            # compile nothing. The Containerfile is the only file that decides
            # the build -- the ffmpeg-builder stage copies nothing from the
            # build context -- and it is also the cache key, so gating on it
            # alone makes every run of the job a real compile.
            ffmpeg_image:
              - 'beam-server/Containerfile'
            compose:
              - *tooling
              - 'compose*.yaml'
              - 'dex/**'

  commits:
    name: Conventional commits
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: jdx/mise-action@v4

      - name: Check commit messages
        env:
          CONVCO_REV: ${{ github.event.pull_request.base.sha }}..${{ github.event.pull_request.head.sha }}
        run: mise run commits:check

      # A squash merge lands the PR title as the commit release-please parses.
      - name: Check pull request title
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: mise run commits:check-title

  # Warms the shared /opt/ffmpeg cache so clippy, test, and openapi restore it
  # rather than each building FFmpeg from source.
  ffmpeg:
    name: Build FFmpeg
    needs: [changes]
    if: needs.changes.outputs.rust == 'true' || needs.changes.outputs.ts == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-ffmpeg

  rust-fmt:
    name: Rust formatting
    needs: [changes]
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      - run: mise run rust:fmt

  rust-lockfile:
    name: Cargo.lock is current
    needs: [changes]
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      - run: mise run rust:lockfile

  ffmpeg-version:
    name: FFmpeg pinned consistently
    needs: [changes]
    if: needs.changes.outputs.ffmpeg == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - run: mise run check:ffmpeg-version

  # ffmpeg-version above only compares the pinned strings; it never compiles.
  # A green run of this job asserts exactly one thing: that *the change to
  # beam-server/Containerfile in this pull request* still builds the FFmpeg
  # stage on `debian:trixie-slim`, a different distro and apt set from the
  # ubuntu-latest build in setup-ffmpeg. Before it, the only job that built
  # that file was release.yml's `images` -- gated on
  # `release_created == 'true'`, so a broken FFmpeg pin surfaced after the tag
  # and the GitHub Release already existed.
  #
  # It asserts no more than that, and the scope is deliberate. The layer cache
  # below is keyed on the Containerfile with no restore-keys, so a run where
  # that file is unchanged replays every layer and compiles nothing -- which is
  # why the trigger is the `ffmpeg_image` filter (the Containerfile alone) and
  # not the broader `ffmpeg` one. In particular this job cannot catch apt-set
  # drift under an unchanged Containerfile: buildkit keys
  # `RUN apt-get update && apt-get install ...` on the command string, not on
  # what apt resolves that day, so the cache masks exactly that class.
  #
  # Only the ffmpeg-builder stage, not the whole image: the Rust build above it
  # is already covered by rust-clippy and rust-test, and redoing it here would
  # cost far more than it proves.
  ffmpeg-build:
    name: FFmpeg container stage builds
    needs: [changes]
    if: needs.changes.outputs.ffmpeg_image == 'true'
    runs-on: ubuntu-latest
    env:
      BEAM_CONTAINER_CACHE_DIR: /tmp/ffmpeg-builder-cache
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      # The default `docker` driver cannot export a cache; the container driver
      # this installs can. Same builder release.yml's images job uses.
      - uses: docker/setup-buildx-action@v4

      # Keyed on the Containerfile, matching the job's trigger: the commit that
      # edits the FFmpeg stage misses the cache and genuinely rebuilds, which is
      # the whole point of the job, while a re-run or a later push that leaves
      # the file alone restores it. There are no restore-keys, so a near miss
      # never silently turns the build into a partial replay; an exact key hit
      # saves nothing back, so the entry does not grow across runs.
      - name: Cache the ffmpeg-builder layers
        uses: actions/cache@v6
        with:
          path: ${{ env.BEAM_CONTAINER_CACHE_DIR }}
          key: ffmpeg-builder-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('beam-server/Containerfile') }}

      - run: mise run check:ffmpeg-build

  compose-invariants:
    name: Compose invariants
    needs: [changes]
    if: needs.changes.outputs.compose == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - run: mise run check:compose-invariants

  rust-deny:
    name: Supply-chain audit
    needs: [changes]
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      - run: mise run rust:deny

  rust-clippy:
    name: Rust lint
    needs: [changes, ffmpeg]
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    env:
      # Link the system FFmpeg that setup-ffmpeg provides, rather than vendoring
      # and statically compiling one (which is the local-dev default).
      BEAM_CARGO_FEATURES: ""
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      - uses: ./.github/actions/install-rust-build-deps
      - uses: ./.github/actions/setup-ffmpeg
      - run: mise run rust:clippy

  rust-test:
    name: Rust tests and coverage
    needs: [changes, ffmpeg]
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    env:
      BEAM_CARGO_FEATURES: ""
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      - uses: ./.github/actions/install-rust-build-deps
      - uses: ./.github/actions/setup-ffmpeg

      # Gates lines, regions, and functions. Regions stand in for branches:
      # cargo-llvm-cov's --branch needs nightly, and rust-toolchain.toml pins
      # stable. Thresholds are calibrated a few points under measured (see
      # mise.toml and docs/testing.md). Ratchet them up over time; do not relax
      # one to make a red pull request pass.
      - run: mise run rust:coverage

      - name: Upload coverage report
        uses: actions/upload-artifact@v7
        with:
          name: rust-coverage
          path: lcov.info
          retention-days: 7

  rust-test-pg:
    name: Rust Postgres integration tier
    needs: [changes, ffmpeg]
    if: needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    env:
      BEAM_CARGO_FEATURES: ""
      # Same engine and credentials as compose.dependencies.yaml, so the tier
      # behaves identically locally and here.
      BEAM_TEST_DATABASE_URL: postgres://beam:password@localhost:5432/beam
    services:
      postgres:
        image: postgres:18.6
        env:
          POSTGRES_USER: beam
          POSTGRES_PASSWORD: password
          POSTGRES_DB: beam
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U beam"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      - uses: ./.github/actions/install-rust-build-deps
      - uses: ./.github/actions/setup-ffmpeg

      # Deliberately outside `mise run ci` and the pre-push hook: this is the one
      # tier that needs infrastructure, and NFR-201 says the default run must not.
      - run: mise run rust:test:pg

  openapi:
    name: Generate OpenAPI artifacts
    needs: [changes, ffmpeg]
    # Also on Rust changes: the spec is generated from the server's route
    # annotations, so a Rust-only change is exactly how it drifts.
    if: needs.changes.outputs.ts == 'true' || needs.changes.outputs.rust == 'true'
    runs-on: ubuntu-latest
    env:
      BEAM_CARGO_FEATURES: ""
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      - uses: ./.github/actions/install-rust-build-deps
      - uses: ./.github/actions/setup-ffmpeg
      - uses: ./.github/actions/bun-install

      - run: mise run codegen:openapi

      # `beam-web/openapi.json` is committed, and it is the contract every
      # client is generated from. Fail when it no longer matches what the
      # server serves.
      - run: mise run codegen:openapi:check

      - name: Upload OpenAPI artifacts
        uses: actions/upload-artifact@v7
        with:
          name: openapi-artifacts
          path: |
            beam-web/openapi.json
            beam-web/src/api.gen.ts
          retention-days: 7

  ts-check:
    name: Biome check
    needs: [changes]
    if: needs.changes.outputs.ts == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: ./.github/actions/bun-install
      # biome.json excludes the generated api.gen.ts, so this needs no OpenAPI.
      - run: mise run ts:check

  ts-typecheck:
    name: TypeScript type check
    needs: [changes, openapi]
    # TODO(#146): re-enable once beam-web is regenerated against the
    # Kynos contract. `api.gen.ts` cannot be regenerated until the TypeScript
    # toolchain reads OpenAPI 3.2 (see the codegen:openapi task), and every
    # schema key it names was renamed by the port. A skipped job still
    # satisfies ci-ok, which passes on success-or-skipped.
    if: false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: ./.github/actions/bun-install
      - uses: actions/download-artifact@v8
        with:
          name: openapi-artifacts
          path: beam-web
      - run: mise run ts:typecheck

  ts-test:
    name: Web tests
    needs: [changes]
    # TODO(#146): only ts-typecheck and ts-build stay off. This job does not
    # need `api.gen.ts` to exist -- vitest resolves through Vite, which never
    # type-checks -- so it neither waits on the openapi job nor downloads its
    # artifact. It runs the same set `ts:test` in the `ci` mise task and the
    # pre-push hook enforce, plus the coverage thresholds in vitest.config.ts.
    if: needs.changes.outputs.ts == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: ./.github/actions/bun-install
      - run: mise run ts:coverage

      - name: Upload coverage report
        uses: actions/upload-artifact@v7
        with:
          name: web-coverage
          path: "**/coverage"
          if-no-files-found: ignore
          retention-days: 7

  ts-build:
    name: Web build
    needs: [changes, openapi]
    # TODO(#146): re-enable once beam-web is regenerated against the
    # Kynos contract. `api.gen.ts` cannot be regenerated until the TypeScript
    # toolchain reads OpenAPI 3.2 (see the codegen:openapi task), and every
    # schema key it names was renamed by the port. A skipped job still
    # satisfies ci-ok, which passes on success-or-skipped.
    if: false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: ./.github/actions/bun-install
      - uses: actions/download-artifact@v8
        with:
          name: openapi-artifacts
          path: beam-web
      # beam-docs is built by the docs-build job, under its own path filter.
      - run: mise run ts:build:web

  docs-build:
    name: Docs site build
    needs: [changes]
    if: needs.changes.outputs.docs == 'true'
    runs-on: ubuntu-latest
    steps:
      # Starlight's lastUpdated reads git history; a shallow clone renders wrong
      # "Last updated" dates rather than failing, so the error would ship silently.
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: jdx/mise-action@v4
      - uses: ./.github/actions/bun-install
      - run: mise run docs:build


  # The Android jobs are JVM-only. An emulator in CI needs nested
  # virtualisation that hosted runners do not reliably provide, and would make
  # the required check flaky; the screenshot tests run under Robolectric
  # instead, which renders real pixels on the JVM.
  #
  # Each job cross-compiles the Rust core, because the Kotlin bindings are
  # generated from the built library rather than from source -- they can only
  # ever describe a .so that actually exists.
  android-lint:
    name: Android lint
    needs: [changes]
    if: needs.changes.outputs.android == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      - uses: ./.github/actions/setup-android
      - uses: gradle/actions/setup-gradle@v6
      - run: mise run android:lint

  android-test:
    name: Android tests
    needs: [changes]
    if: needs.changes.outputs.android == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      - uses: ./.github/actions/setup-android
      - uses: gradle/actions/setup-gradle@v6
      - run: mise run android:test

      - name: Upload test reports
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: android-test-reports
          path: beam-android/**/build/reports/tests
          if-no-files-found: ignore
          retention-days: 7

  android-screenshot:
    name: Android screenshots
    needs: [changes]
    if: needs.changes.outputs.android == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      - uses: ./.github/actions/setup-android
      - uses: gradle/actions/setup-gradle@v6
      - run: mise run android:screenshot

      # The diffs are the whole point of a failed screenshot check: without
      # them a reviewer knows only that something moved, not what.
      - name: Upload screenshot diffs
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: android-screenshot-diffs
          path: beam-android/**/build/outputs/roborazzi
          if-no-files-found: ignore
          retention-days: 7

  android-build:
    name: Android build
    needs: [changes]
    if: needs.changes.outputs.android == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      - uses: ./.github/actions/setup-android
      - uses: gradle/actions/setup-gradle@v6
      - run: mise run android:build

  # The only job that exercises R8. Every other Android job is debug-only, and
  # minification and resource shrinking are configured on the release build type
  # alone, so without this the release path is first built by release.yml --
  # after the tag is cut. See the android:minify task for what it does and does
  # not cover.
  android-minify:
    name: Android minify
    needs: [changes]
    if: needs.changes.outputs.android == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      - uses: ./.github/actions/setup-android
      - uses: gradle/actions/setup-gradle@v6
      - run: mise run android:minify

  # The Apple jobs need macOS runners, which are free for a public repository
  # but slower and scarcer than the Linux ones -- hence the path filter, and
  # hence four focused jobs rather than one that redoes the cross-compile four
  # times.
  #
  # Each cross-compiles the Rust core, because the Swift bindings are generated
  # from the built library rather than from source: they can only ever describe
  # an XCFramework that actually exists.
  #
  # Xcode is pinned explicitly. The snapshot references are pixel comparisons
  # recorded against one renderer, and a runner image that silently moved to a
  # newer Xcode would fail them for a reason that has nothing to do with the
  # change under test.
  apple-lint:
    name: Apple lint
    needs: [changes]
    if: needs.changes.outputs.apple == 'true'
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.0"
      - uses: jdx/mise-action@v4
      - run: mise run apple:lint

  apple-test:
    name: Apple tests
    needs: [changes]
    if: needs.changes.outputs.apple == 'true'
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.0"
      - uses: jdx/mise-action@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      # The macOS runner image ships Xcode with the macOS platform only: every
      # other destination reports "iOS 26.0 is not installed" and
      # `xcodebuild`'s available-destinations list comes back empty. This
      # downloads the simulator runtime, and is a no-op when it is already
      # present.
      - name: Install the iOS simulator runtime
        run: |
          xcodebuild -downloadPlatform iOS
          # Printed so that if the pinned device name ever stops existing, the
          # log says what does rather than only that nothing matched.
          xcrun simctl list devices available

      - run: mise run apple:test

  apple-snapshot:
    name: Apple snapshots
    needs: [changes]
    if: needs.changes.outputs.apple == 'true'
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.0"
      - uses: jdx/mise-action@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      # The macOS runner image ships Xcode with the macOS platform only: every
      # other destination reports "iOS 26.0 is not installed" and
      # `xcodebuild`'s available-destinations list comes back empty. This
      # downloads the simulator runtime, and is a no-op when it is already
      # present.
      - name: Install the iOS simulator runtime
        run: |
          xcodebuild -downloadPlatform iOS
          # Printed so that if the pinned device name ever stops existing, the
          # log says what does rather than only that nothing matched.
          xcrun simctl list devices available

      - run: mise run apple:snapshot

      # The diffs are the whole point of a failed snapshot check: without them
      # a reviewer knows only that something moved, not what.
      - name: Upload snapshot diffs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: apple-snapshot-diffs
          path: |
            beam-apple/Packages/BeamKit/Tests/SnapshotTests/__Snapshots__
            ~/Library/Developer/Xcode/DerivedData/**/Logs/Test/*.xcresult
          if-no-files-found: ignore
          retention-days: 7

  apple-build:
    name: Apple build
    needs: [changes]
    if: needs.changes.outputs.apple == 'true'
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.0"
      - uses: jdx/mise-action@v4
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      # The macOS runner image ships Xcode with the macOS platform only: every
      # other destination reports "iOS 26.0 is not installed" and
      # `xcodebuild`'s available-destinations list comes back empty. This
      # downloads the simulator runtime, and is a no-op when it is already
      # present.
      - name: Install the iOS simulator runtime
        run: |
          xcodebuild -downloadPlatform iOS
          # Printed so that if the pinned device name ever stops existing, the
          # log says what does rather than only that nothing matched.
          xcrun simctl list devices available

      - run: mise run apple:build

  # The single required status check. Branch protection should require this job
  # and nothing else: it is the only one that always runs, and it distinguishes
  # "skipped by a path filter" (fine) from "failed" (not fine).
  ci-ok:
    name: CI
    if: always()
    needs:
      - changes
      - commits
      - ffmpeg
      - rust-fmt
      - rust-lockfile
      - ffmpeg-version
      - ffmpeg-build
      - compose-invariants
      - rust-deny
      - rust-clippy
      - rust-test
      - rust-test-pg
      - openapi
      - ts-check
      - ts-typecheck
      - ts-test
      - ts-build
      - docs-build
      - android-lint
      - android-test
      - android-screenshot
      - android-build
      - android-minify
      - apple-lint
      - apple-test
      - apple-snapshot
      - apple-build
    runs-on: ubuntu-latest
    steps:
      - name: Fail unless every job succeeded or was skipped
        env:
          RESULTS: ${{ toJSON(needs) }}
        run: |
          echo "$RESULTS" | jq -r 'to_entries[] | "\(.value.result)\t\(.key)"' | sort
          echo "$RESULTS" | jq -e '
            to_entries
            | map(select(.value.result != "success" and .value.result != "skipped"))
            | length == 0
          ' > /dev/null
