# =============================================================================
# Beam Web (Frontend) Environment Variables
# =============================================================================
# Environment variables for the React frontend application.
# These variables are prefixed with "C_" to indicate they are client-side.
#
# Copy this file to `.env` for local development.
# =============================================================================

# Application title displayed in the browser tab and UI
# Default: "Beam"
C_APP_TITLE=Beam

# URL of the beam-stream backend server
# This is used by the frontend to make GraphQL and streaming requests
# For local development: http://localhost:8000
# For production: https://your-backend-domain.com
C_STREAM_SERVER_URL=http://localhost:8000
